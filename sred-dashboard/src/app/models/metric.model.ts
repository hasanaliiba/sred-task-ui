/**
 * The metric the dashboard is viewed through (Feature: Show Hours / Expenditures /
 * Credits). Drives the metric-aware figures on the Analytics page.
 */
export type Metric = 'hours' | 'expenditure' | 'credit';
